import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function Tienda() {
  return (
    <ScreenPlaceholder
      title="Tienda"
      owner="Edgar"
      source="Web - Client/Tienda.jsx"
      note="Navega al detalle con router.push(`/(client)/tienda/${id}`)"
    />
  );
}
