import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function Carrito() {
  return (
    <ScreenPlaceholder
      title="Carrito de compras"
      owner="Edgar"
      source="Web - Client/CarritoCliente.jsx"
      note="Botón de pagar -> router.push('/(client)/carrito/pago')"
    />
  );
}
