import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function Tienda() {
  return (
    <ScreenPlaceholder
      title="Tienda"
      owner="Edgar"
      source="Web - Client/Tienda.jsx"
      note="Navega al detalle con navigation.navigate('ProductoDetalle', { id })"
    />
  );
}
