import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function PagoCarrito() {
  return (
    <ScreenPlaceholder
      title="Pago del carrito"
      owner="Edgar"
      source="Web - Client/PagoCarritoCliente.jsx"
      note="Consume useAuth() + useCarShop() + usePagoCarrito()."
    />
  );
}
